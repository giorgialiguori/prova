/**
 * preload component
 *
 * @param UIComponent
 * @returns
 */
sap.ui.define(
	["sap/ui/core/UIComponent"],
	function(UIComponent)
	{
		"use strict";

		var preload = sap.ui.core.UIComponent.extend("com.giorgia.provaUI5.Component-preload", {});
		return preload;
	}
);
